import { AddTooltipDirective } from './add-tooltip.directive';

describe('AddTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new AddTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
